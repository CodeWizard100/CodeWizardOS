name: Run build

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

   

    # Additional steps for testing or other tasks
    # Add any additional steps here as needed