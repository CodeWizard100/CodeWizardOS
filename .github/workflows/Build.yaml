name: Run build

on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
      steps:
    - uses: actions/checkout@v2
    - uses: open-watcom/setup-watcom@v0
  with:
      version: "2.0"
    - run: |
      wcl386 --version

